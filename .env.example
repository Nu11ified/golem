# Example environment configuration for go-react-framework

# Port for the Go orchestrator server (internal container port)
GO_PORT=8080

# Port for the Node renderer server (internal container port)
NODE_PORT=3001

# Host for the Node renderer (used by Go orchestrator to call Node renderer)
NODE_RENDERER_HOST=localhost

# You can copy this file to .env and adjust the values as needed